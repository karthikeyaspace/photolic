interface SidebarFormTypes {
  prompt: string;
  creativity: string;
  filmType: string;
  emotion: string;
  cameraPosition: string;
  place: string;
  numOutputs: number;
  aspectRatio: string;
  useSeed: boolean;
  seed: string;
}

export type { SidebarFormTypes };
